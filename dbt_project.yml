name: dbt_order_analytics
version: 1.0.0
config-version: 2

profile: dbt_learn

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_order_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
